import SwiftUI

enum Theme {
    static let background = Color(red: 0x0B / 255, green: 0x0F / 255, blue: 0x0E / 255)
    static let card = Color(red: 0x16 / 255, green: 0x1C / 255, blue: 0x1A / 255)
    static let accent = Color(red: 0x19 / 255, green: 0xC3 / 255, blue: 0x7D / 255)
    static let secondaryText = Color(white: 0.62)
    static let cornerRadius: CGFloat = 20
}
